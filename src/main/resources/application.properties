spring.application.name=demo

# Server & Proxy Configuration (Cloudflare Tunnel / Reverse Proxy support)
server.forward-headers-strategy=framework

# CORS Configuration
cors.allowed-origins=https://task-list.ch,https://www.task-list.ch,http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000

